interface:
  display_name: "PR Review"
  short_description: "Review monorepo pull requests with affected analysis and risk flags"
  default_prompt: "Use the pr-review skill to summarize impact, identify owners, check risky paths, and select the smallest meaningful verification gates."
